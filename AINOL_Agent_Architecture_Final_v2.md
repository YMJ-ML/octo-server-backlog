# octo-server 产品管家 Agent —— 架构设计

> 考试项目：octo-server  
> 设计日期：2026-09-08  
> 核心原则：(1) 只读取 octo-server 源码，不修改目标仓库；(2) 所有周期性工作由 cron 自驱，不依赖人工触发；(3) 三个 bot 各自在群里以自己的名字发言

---

## 1. 系统概览

本系统由 **3 个 LLM Agent + 1 个 cron 调度层** 组成，为 octo-server 项目提供"产品管家"服务：

- **随时答产品问题**：基于本地只读源码副本给出可核验的结论，附文件路径与行号
- **收口反馈**：把群里的反馈结构化沉淀到独立需求池仓库
- **推进需求**：认领 → 补 PRD → 评审 → 按评审意见迭代，形成闭环
- **主动同步**：需求池状态变化时自动在群里通报

## 2. 总体架构

```
 ┌─────────────────────────────────────┐
 │ Octo 群（团队 / 你 / 你的 Bots）     │
 └───┬───────────────┬───────────────┬───┘
     │ 问答/通报     │ 进展同步       │ 评审结论
 ┌───▼──────┐ ┌──────▼──────┐ ┌──────▼──────┐
 │octo产品管家 │ │ octo PRD   │ │octo Review  │
 │A1:问答+收单 │ │A2:PRD撰写  │ │A3:评审     │
 └───┬──────┘ └──────┬──────┘ └──────┬──────┘
     │ 认领→PRD      │ 评审↔打回     │
     └────────┬──────┴──────┬────────┘
              │ Issue 状态机 │
         ┌────▼─────────────▼──────┐
         │ 需求池仓库（唯一真相源）  │
         │ • Issue 状态机           │
         │ • Label 体系             │
         │ • state.json（快照）     │
         │ • EXECUTION_LOG.md（日志）│
         └────────────────────────────┘
              ▲ 外部人员改动（关单/打标签）
 ┌────────────┴────────────────────────────┐
 │ (K) Cron 调度层（系统级，非 LLM）       │
 │ • run_sync.sh（每 5 分钟）              │
 │   → 增量同步需求池 + 检测外部变更      │
 │ • run_pm.sh（每 15 分钟）              │
 │   → 推进 PM 链路（认领→PRD→评审）    │
 │ • 事件快路径（近实时）                  │
 │   → A1 收单后立即触发 A2 接管          │
 └─────────────────────────────────────────┘

（本地只读代码副本：octo产品管家 答产品问题用）
```

---

## 3. 三个 Agent 的职责

| Bot | 名字 | 群里发言 | 核心职责 | 触发方式 |
|-----|------|--------|--------|---------|
| A1 | `octo产品管家` | ✅ 自己发 | ① 答产品问题（带源码引用）② 收反馈→建 issue ③ 回报群里的外部变更 | 被 @/被提问；被 cron 触发回报 |
| A2 | `octo PRD` | ✅ 自己发 | 认领需求→补 PRD（只写 What）→按打回的结构化理由改稿；PRD 进展自己同步群 | 被 cron 调用 |
| A3 | `octo Review` | ✅ 自己发 | 对"待评审"单做 review→通过/打回+结构化理由；评审结论自己同步群 | 被 cron 调用 |

### 3.1 octo产品管家（A1）

**职责：**
1. **问答** — @问问题 → 查本地只读源码 → 回答并附引用 `来源: <路径>#L<起>-L<止>`
2. **收单** — 群里反馈 → 建 issue 到需求池 → 打 `triage` label
3. **回报** — Cron 检测外部变更 → 在群里通报（@主考 + @相关人）

**输出规范：**
- 每条消息都 @主考（无条件）
- 涉及具体需求时额外 @相关人
- 无变化不发言（空跑只进日志）

### 3.2 octo PRD（A2）

**职责：**
1. **认领** — 拉取 `triage` issue → 置为 `accepted`
2. **补 PRD** — 读 issue 描述 → 补成 PRD（只写 What，不写 How）→ 置 `prd`
3. **改稿** — 根据 A3 的结构化打回理由改 PRD → 重新置 `review`
4. **同步进展** — PRD 每次推进都在群里同步摘要（@主考）

**输出规范：**
- 每条消息都 @主考
- 空跑只进日志

### 3.3 octo Review（A3）

**职责：**
1. **评审** — 拉取 `review` issue → 评审 PRD
2. **通过** — 置 `approved` → `done`
3. **打回** — 输出**结构化理由**（如 `rejected/验收标准不可测`），置 `rejected/<原因>`
4. **同步结论** — 评审结果在群里同步摘要（@主考）

**输出规范：**
- 每条消息都 @主考
- 打回必须有结构化理由，否则无效
- 空跑只进日志

---

## 4. Cron 调度层（系统自驱的核心）

> **核心原则**：所有周期性工作由 cron 定时触发，不依赖任何人工指令。没有"去扫一下"的按钮是人的必经动作。

### 4.1 两个 Cron 脚本

**`run_sync.sh`（每 5 分钟）**
1. 用 GitHub REST API 拉取需求池增量（只用 list/events，避开 Search 限流）
2. 与本地 `state.json` 快照做 diff，检测真实变化
3. 若有变化 → 调用 A1 在群里通报（@主考 + @相关人）
4. 若无变化 → 只写日志，不发任何过程消息
5. 更新 `state.json` 快照和 `EXECUTION_LOG.md`

**`run_pm.sh`（每 15 分钟）**
1. 拉取 `triage` issue → 交 A2 认领（置 `accepted`）
2. 拉取 `accepted` 无 PRD 的 issue → 交 A2 补 PRD（置 `prd`）
3. 拉取 `prd` issue → 置 `review` 并指派 A3 评审
4. 拉取 `review` issue → 交 A3 评审：通过置 `approved`；打回置 `rejected/<原因>`
5. 拉取 `rejected/*` issue → 交 A2 改稿（置 `prd` 重新 review）
6. 若本轮有状态推进 → A2/A3 各自在群里同步摘要；空跑只留日志

### 4.2 事件快路径（近实时）

- A1 收单完成后，系统立即再跑一次 `run_pm.sh --issue=<n>` 只处理该单
- A2 秒级接管，不必等最长 15 分钟
- **注意**：事件触发由系统自动发起，不是人喊"去扫"，不违反"cron 自驱"原则

---

## 5. 需求池仓库：唯一真相源

独立 public GitHub 仓库同时承担：反馈归档、需求生命周期、外部变更入口。

### 5.1 Issue 状态机（对应 PM 链路）

```
反馈进池 → triage(待分诊) → accepted(已认领) → prd(已有PRD)
                                           ↓
                              review(待评审) → approved(通过) → done
                                 ↑ ↓
                         rejected/<原因>(打回)
                                 ↓
                              prd(改稿)

外部可随时：closed / closed as wontfix → wontfix label
```

### 5.2 Label 体系（三类齐全）

| 类别 | 示例 | 含义 |
|------|------|------|
| type | `type/bug` `type/feature` `type/question` | 反馈类型 |
| priority | `P0` `P1` `P2` | 优先级 |
| status | `triage` `accepted` `prd` `review` `approved` `rejected/<原因>` `wontfix` `done` | 生命周期阶段 |

### 5.3 持久化与可观测性

**`state.json`** — 需求池每个 issue 的上一次快照（label、open/closed），供 cron 做增量 diff

**`EXECUTION_LOG.md`** — 每次 cron 运行追加一行：时间、扫到条目数、是否空跑、执行的动作
- 直接证据："系统确实在定时自驱"
- 排查工具：哪一轮漏处理了什么

---

## 6. 本地只读代码副本（关键设计）

### 6.1 为什么必须本地只读副本

1. **行号引用要精确高频** — 结论格式 `来源: <路径>#L<起>-L<止>`，常需跨文件精读，本地毫秒级无限次；远程 API 不耐
2. **引用预核验** — 路径存在/行号在区间/文本支撑结论，只能基于本地文件系统
3. **目标仓库只读红线** — clone 到本地只读目录物理保证"读遍源码却不写回"
4. **README 不可信** — git checkout 锁定版本，问答可复现可追责
5. **规避限流** — GitHub Search 30/分、REST 5000/小时；本地零成本零限流
6. **稳定性** — 不依赖外部网络，考试运行时稳定
7. **安全** — clone 公开仓库，工具调用无需 token 进 prompt

### 6.2 技术可行性确认 ✅

- octo-server 源码已在 `/tmp/octo-server`（1761 个 Go 文件）
- 本地可稳定读取、grep、提取行号
- 预核验脚本可确认路径+行号真实存在
- Agent 只做**按需检索/读取**，不全量灌进上下文

---

## 7. 群内消息与输出硬约束

1. **@主考必带** — 每一条群消息无条件 @主考，不因"只是顺带同步"而省略
2. **相关人必带** — 涉及具体需求/变更时额外 @相关人
3. **无变化不发言** — 没有检测到变更、没有要转述的结论，只进日志
4. **如实转述** — 已修复 ≠ 未复现 ≠ 不做（wontfix），用 label 精确表达
5. **答不上来就认** — 知识库没有的结论，回"我不确定" + 指出该找谁 + 说明要补哪块知识
6. **凭证不出门** — token 走环保变量，绝不进群、绝不进 git

---

## 8. 引用核验机制

- A1 答产品问题前用脚本验证：路径相对仓库根存在、行号区间在文件范围内、该段文本确实支撑结论
- 核验不过的结论不允许发出 — **从机制上杜绝编造引用**
- 每条结论格式：`来源: modules/bot_api/bot_api.go#L30-L50`

---

## 9. 打回结构化硬约束

- A3 打回必须输出**结构化理由**，如 `rejected/验收标准不可测`、`rejected/缺用户场景`
- 自然语言打回视为无效，循环回 A3 重打
- A2 能确定性对症改稿，不是 LLM 猜测

---

## 10. 部署要点

- **平台**：任意能稳定跑常驻 cron 与 LLM 调用的环境
- **群集成**：三个 bot 各自持有群身份，分别以自己名字发言
- **代码副本**：`main` 分支浅克隆到本地只读目录
- **Cron 宿主**：独立进程/机器，即使 Agent 进程重启，cron 依然按表触发

---

## 11. 架构完整性检查

- [x] 3 个 Bot 职责边界清晰
- [x] 考官操作驱动流程
- [x] Cron 定期自动扫描（系统自驱）
- [x] 事件快路径（近实时 A2 接管）
- [x] 有产出才发消息（@主考 + 结构化）
- [x] 引用预核验（杜绝造假）
- [x] 打回结构化（确定性改稿）
- [x] 本地只读代码副本（可行性 ✅）
- [x] 需求池即真相源（state.json + EXECUTION_LOG）
- [x] 核心标准落地

---

**架构设计完成！** 🚀
